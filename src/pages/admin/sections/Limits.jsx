import { ComingSoon } from '../AdminKit';

export default function Limits() {
  return <ComingSoon title="Limits" note="Per-plan resource limits (accounts, characters, API calls). Managed by your subscription tier." />;
}
