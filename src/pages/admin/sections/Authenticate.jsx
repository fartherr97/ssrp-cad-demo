import { ComingSoon } from '../AdminKit';

export default function Authenticate() {
  return <ComingSoon title="Authenticate" note="Re-authenticate this session against your community owner credentials." />;
}
