import { ComingSoon } from '../AdminKit';

export default function CustomRecords() {
  return <ComingSoon title="Custom Records" note="Define custom record templates (gun registry, pet licenses, etc.). Managed alongside your record schemas." />;
}
