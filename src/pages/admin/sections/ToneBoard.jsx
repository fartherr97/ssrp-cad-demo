import { ComingSoon } from '../AdminKit';

export default function ToneBoard() {
  return <ComingSoon title="Tone Board" note="Configure the live tone board used by dispatch to page units and stations." />;
}
