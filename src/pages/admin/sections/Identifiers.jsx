import { ComingSoon } from '../AdminKit';

export default function Identifiers() {
  return <ComingSoon title="Identifiers" note="Player identifier mappings (Discord, game license, Steam) are synced from your server's identifier feed." />;
}
