import { ComingSoon } from '../AdminKit';

export default function Emails() {
  return <ComingSoon title="Custom Community Emails" note="Customize the transactional emails (welcome, whitelist, password reset) sent to members." />;
}
